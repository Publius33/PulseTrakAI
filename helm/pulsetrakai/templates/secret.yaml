apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pulsetrakai.fullname" . }}-secret
type: Opaque
data:
  # Placeholder: Do NOT commit real secrets. Populate via CI or Kubernetes secrets.
  # EXAMPLE_KEY: "REPLACE_BASE64"

© PUBLIUS33™ — PulseTrakAI™ — All Rights Reserved.
